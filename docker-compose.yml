version: '3.8'

services:
  sentinel-view:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sentinel-view-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
